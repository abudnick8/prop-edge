import { createRoot } from "react-dom/client";
import App from "./App";
import "./index.css";

// Light mode — cream background, dark navy text (baseball scorecard theme)

createRoot(document.getElementById("root")!).render(<App />);
