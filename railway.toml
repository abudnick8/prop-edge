# PropEdge Railway Config — updated 2026-03-15
[build]
builder = "nixpacks"
buildCommand = "npm run build"

[deploy]
startCommand = "node dist/index.cjs"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
